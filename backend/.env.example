# 🔧 Variables de Entorno para Deployment
# Copia este archivo a .env y personaliza los valores según tu setup

# ========================================
# 🚀 DEPLOYMENT CONFIGURATION
# ========================================

# GitHub Repository (formato: owner/repo)
# Default: zsantiago8712/coding_challenge
GITHUB_REPOSITORY=zsantiago8712/coding_challenge

# Branch a desplegar
# Default: main
BRANCH_NAME=main

# Región AWS (opcional, se detecta automáticamente)
# Default: us-east-1
# CDK_DEFAULT_REGION=us-east-1

# Account AWS (opcional, se detecta automáticamente)
# CDK_DEFAULT_ACCOUNT=123456789012

# ========================================
# 🌱 SEEDING CONFIGURATION
# ========================================

# Nombre de la tabla DynamoDB
# Default: Notes
DYNAMODB_TABLE_NAME=Notes

# Cantidad de notas para seeding
# Default: 50
SEED_COUNT=50

# ========================================
# 🔐 GITHUB TOKEN (OPCIONAL)
# ========================================

# Token de GitHub para conectar automáticamente el repo
# Si no lo configuras, tendrás que conectar manualmente desde Amplify Console
GITHUB_TOKEN=your_github_token_here

# Nombre del parámetro en AWS Systems Manager donde guardar el token
# GITHUB_TOKEN_PARAM_NAME=/amplify/github/token

# ========================================
# � FRONTEND ENVIRONMENT VARIABLES
# ========================================

# Estas variables se configuran automáticamente en Amplify,
# pero puedes usarlas para desarrollo local del frontend

# GraphQL API Endpoint (se genera automáticamente después del deploy)
# NEXT_PUBLIC_GRAPHQL_ENDPOINT=https://xxxxxxxxxx.appsync-api.us-east-1.amazonaws.com/graphql

# AWS Region
NEXT_PUBLIC_AWS_REGION=us-east-1

# Cognito Identity Pool ID (se genera automáticamente después del deploy)
# NEXT_PUBLIC_IDENTITY_POOL_ID=us-east-1:xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# ========================================
# �🏷️ TAGS (OPCIONAL)
# ========================================

# Environment tag
# Default: se determina por BRANCH_NAME (main=production, otros=development)
# ENVIRONMENT=production

# Project tag
# Default: NotesSentimentApp
# PROJECT_NAME=NotesSentimentApp
